= simple_form_for task,
                  html: { class: 'form-inline task-form' } do |f|
  = f.input :title, label: false
  = f.input :taskable_id, as: :hidden
  = f.input :taskable_type, as: :hidden
  = f.input :deadline, as: :string,
                       input_html: { class: "js-datepicker" },
                       placeholder: "Deadline Date",
                       label: false
  = f.association :users, placeholder: "Assign People",
                          input_html: { class: 'js-users',
                                        multiple: true },
                          label: false

  .control-group
    .controls
      = f.button :submit, class: 'btn-success'
